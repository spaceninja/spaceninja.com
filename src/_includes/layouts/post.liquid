---
layout: layouts/base.njk
prism: true
---
<article>

<header>
  <h1>{{ title }}</h1>

  <p>
    Posted on
    <time datetime="{{ page.date | htmlDateString }}">
      {{ page.date | readableDate }}
    </time>
  </p>
  <p>It will take {{ content | timeToRead }} to read this</p>
</header>

{% if feature_image %}
  {% include "partials/image.njk"
    src: feature_image
    alt: ""
    loading: "eager"
    classList: "feature-image"
  %}
{% endif %}

{{ content }}

<footer>
  {% if tags | filterTagList | size %}
  <div>
    <h3>Tags</h3>
    <ul class="list--inline">
      {%- for tag in tags | filterTagList | sort -%}
        <li>
        {%- if collections[tag].size > 1 -%}
          <a
            class="tag"
            href="/tags/{{ tag | slugify }}/"
            title="{{ collections[tag].size }} posts with this tag"
          >{{ tag }}</a>
        {%- else -%}
          <span
            class="tag orphan"
            title="Only one post with this tag"
          >{{ tag }}</span>
        {%- endif -%}
        </li>
      {%- endfor -%}
    </ul>
  </div>
  {% endif %}
  {%- if collections.blog %}
  {%- assign previousPost = collections.blog | getPreviousCollectionItem %}
  {%- assign nextPost = collections.blog | getNextCollectionItem %}
  {%- if nextPost or previousPost %}
  <ul class="links-nextprev">
    {%- if previousPost %}<li>Previous: <a href="{{ previousPost.url }}">{{ previousPost.data.title }}</a></li>{% endif %}
    {%- if nextPost %}<li>Next: <a href="{{ nextPost.url }}">{{ nextPost.data.title }}</a></li>{% endif %}
  </ul>
  {%- endif %}
  {%- endif %}
</footer>

</article>
